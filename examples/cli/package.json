{
  "name": "@examples/cli",
  "description": "Example of using the CLI to test a Web Messenger deployment",
  "private": true,
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "execute": "web-messaging-tester example.yml -id $DEPLOYMENT_ID -r $REGION -p 10"
  },
  "dependencies": {
    "@ovotech/genesys-web-messaging-tester-cli": "*"
  }
}
